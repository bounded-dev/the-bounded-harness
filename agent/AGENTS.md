# Global agent instructions

This is the default `AGENTS.md` for **every** pi session on this machine
(`~/.pi/agent` → `pi-harness/agent`). Keep it lean: anything here is injected
into every project's context.

_Stub — content to be filled in deliberately. Harness-maintenance rules live
in the repo-root `AGENTS.md`, which only loads when working on pi-harness
itself._
